<%
/* Rendered at /site.webmanifest — see server/index.js. Built from
   data/profile.json so the installed app name matches the site. */
%><%- JSON.stringify({
  name: profile.identity.name + ' — ' + profile.identity.headline,
  short_name: profile.identity.name,
  description: profile.seo.description,
  start_url: '/',
  scope: '/',
  display: 'standalone',
  background_color: '#06070e',
  theme_color: '#06070e',
  lang: 'en-BD',
  dir: 'ltr',
  categories: ['portfolio', 'business', 'productivity'],
  icons: [
    { src: '/img/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
    { src: '/img/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'any' },
    { src: '/img/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' }
  ]
}, null, 2) %>
